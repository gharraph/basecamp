<h2>Your To do list for <%= link_to @list.project.name,  project_path(@list.project)%>:</h2>
<h1><%= @list.name%></h1></br>

<div class="span12">
	<div class="span6">
	<%= render :partial => "items/items" %>
	</div>

	<div class="span4 offset 6">
	<%= render :partial => "items/embedded_form", :locals => {:item => Item.new, :list => @list} %>
	</div>
</div>
