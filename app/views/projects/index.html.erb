<h1>Your Projects</h1>
<br />
<br />

<div class="span12">
	<div class="span6">
		<div id="projects">	
	<% if current_user %>		
	 <%= render :partial => 'projects/projects' %>
	<% else %>
	<img src="images/sad_kitty.png" /> 
	<% end %>
		</div>	
	</div>
	<% if current_user %>
	<div class="span4 offset 6">
		<% @project = Project.new %>
		<%= render 'form' %>
	</div>
	<% else %>
	<%= button_to 'Log in or sign up', new_user_session_path, :class=>"btn btn-primary btn-large" %>
	<% end %>
</div>

